<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.userapp">

    <application
        android:name=".application"
        android:allowBackup="true"
        android:allowClearUserData="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:requestLegacyExternalStorage="true"
        android:requiredForAllUsers="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UserApp"
        android:usesCleartextTraffic="true"
        tools:targetApi="q">
        <activity android:name=".Activity.SapaShowActivity"></activity>
        <activity android:name=".Activity.SapaDharaActivity" />
        <activity android:name=".Activity.ContactUsActivity" />
        <activity android:name=".Activity.CourceActivity" />
        <activity android:name=".Activity.AboutUsActivity" />
        <activity android:name=".Activity.HomeActivity" />
        <activity
            android:name=".Activity.MainActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".Activity.SplaceScrennActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>